/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: {
    ignoreDuringBuilds: true,
  },
  typescript: {
    ignoreBuildErrors: true,
  },
  images: {
    unoptimized: true,
  },
  async redirects() {
    return [
      {
        source: '/newsletter/:slug',
        destination: '/redirect-newsletter/:slug',
        permanent: true,
      },
    ]
  },
}

export default nextConfig